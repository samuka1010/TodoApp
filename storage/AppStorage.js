import { 
    saveServerSettings, 
    loadServerSettings, 
    clearServerSettings,
} from './app_items/ServerSettings';

import {
    saveSyncInfo,
    loadSyncInfo,
    clearSyncInfo,
} from './app_items/SyncInfo';

import {
    saveUserInfo,
    loadUserInfo,
    clearUserInfo,
} from './app_items/UserInfo';

export default {
    saveServerSettings, 
    loadServerSettings, 
    clearServerSettings,

    saveSyncInfo,
    loadSyncInfo,
    clearSyncInfo,

    saveUserInfo,
    loadUserInfo,
    clearUserInfo,    
}